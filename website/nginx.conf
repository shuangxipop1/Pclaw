upstream pclaw_api {
    server api:3001;
    keepalive 32;
}

upstream pclaw_frontend {
    server frontend:80;
    keepalive 16;
}

server {
    listen 80;
    server_name pclawai.com www.pclawai.com;

    # Static files -> frontend container
    location / {
        proxy_pass http://pclaw_frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        # Static caching
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
            proxy_pass http://pclaw_frontend;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # API -> API container
    location /api/ {
        proxy_pass http://pclaw_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_buffering off;
        proxy_request_buffering off;
        # Timeout settings
        proxy_connect_timeout 10s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # ACME challenge for SSL certs (let's encrypt)
    location /.well-known/acme-challenge/ {
        root /var/www/acme;
    }
}

# HTTPS redirect (if needed)
# server {
#     listen 443 ssl;
#     server_name pclawai.com www.pclawai.com;
#     ssl_certificate /etc/nginx/ssl/pclawai.pem;
#     ssl_certificate_key /etc/nginx/ssl/pclawai.key;
#     return 301 https://$host$request_uri;
# }
